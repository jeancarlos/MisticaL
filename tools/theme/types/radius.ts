type BorderRadius = {
  value: string;
  type: string;
};

export interface Radius {
  avatar: BorderRadius;
  bar: BorderRadius;
  button: BorderRadius;
  checkbox: BorderRadius;
  container: BorderRadius;
  indicator: BorderRadius;
  input: BorderRadius;
  legacyDisplay: BorderRadius;
  popup: BorderRadius;
  sheet: BorderRadius;
  mediaSmall: BorderRadius;
}
